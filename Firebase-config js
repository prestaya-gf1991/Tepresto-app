// firebase.js - PARA TU APP TE PRESTO - YA CON FIRESTORE
import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";
import { getAnalytics } from "firebase/analytics";

// Tu configuración de Firebase (la que ya tienes)
const firebaseConfig = {
  apiKey: "TU_API_KEY_AQUI",
  authDomain: "Te-presto-barahona.firebaseapp.com",
  projectId: "Te- presto-barahona",
  storageBucket: "TU_PROYECTO.appspot.com",
  messagingSenderId: "TU_ID",
  appId: "TU_APP_ID",
  measurementId: "TU_MEASUREMENT_ID" // si lo tienes
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);

// ESTA ES LA LÍNEA QUE TE FALTABA PARA CONECTAR LA BASE DE DATOS
export const db = getFirestore(app);
